/* Online Taxi Booking Service:
Alice uses online taxi booking service to go to the office from his home and uses a samsung mobile online application for booking.
The online booking costs Rs.x for first 'n' kms.
And Rs.y for every km afterward.
Find the cost that is required to pay by Alice.
The distance from Alice's home to office is 'D' kms.*/
#include<stdio.h>
 void main()
 {
     int = x,y,d,n,cost;
     printf("Enter the distance");
     scanf("%d", &d);
     printf("Enter the ")
     printf("For first 10 kms the cost is Rs.50");
     n == 10
     x == 50

     if (d<10) {
        cost =
     }

 }
