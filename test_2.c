#include<stdio.h>
#include<stdlib.h>

struct patient
{
    char pname[20];
    int pid;
    char hospital[50];
    float bill;
    struct patient *next;
};

struct patient* ibeg(struct patient *head)
{
    struct patient *temp, *temp1;
}

struct patient* desc_sort(struct patient *head)
{

}

struct patient* apollo(struct patient *head)
{

}
